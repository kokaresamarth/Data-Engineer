1) what is data Engineering?
=> Converts raw Data into the clean and structured format with the help of Transformation.

2) Data Engineering Workflow?
=> Data Production -> Data Transformation -> Data serving

3) Data Generation?
=> Data is generated by the API, Application, Streaming it is stored in the DataBase like SQL, NoSQL.

4) Data Transformation?
=> Converting Data from DBA to Clean and Structure format and store in the Data warehouse.

5) Data Serving?
=> Insted of serving a bid data we create data model.

6) OLTP/OLAP
=> OLTP
-------
-> Online Transaction Processing
-> It help to Writes and Updatas the database
-> Popular database are MySQL and PostSQL
-> OLTP is managing by DBA using Modeling like 1NF, 2NF, 3NF

=> OLAP
-------
-> Onine analytical Processing
-> It is also known as Data Warehousess
-> It is use to Business purpose

7) ETL
-> Ectract -> Transform -> Load

8) Data WareHouse and Layers
=>Layers
  => Staging layars
	-> Transient layers
	-> Persistent
  => Core Layers

9) Incremental Loading
=> Incremental loading reduces data processing time and cost by loading only new or changed records instead of reloading the entire dataset every time.

10) Dimensional modeling
=> It is is modeling technique inwhich we store our data in the form of fact(store Numaric value) and dimensions(store nameing information)
Types of Dimensional Model
--------------------------
=> Star Schema
=> Snowflake Schema

11) Slowly Changing Dimensions(SCD)?
=> - TYPE-0 -> Table will not change
- TYPE-1 ->  Table Will Store like a UPSEART
- TYPE-2 -> Table will store the history with date columns
- TYPE-3 -> Table will Preserve history without date columns

12) Data Lake?
=> It is Store the all type of raw data (Semi and structure and unstructure data)

13) Data Lakehouse?
=> It is Combination of data lake and data warehouse into single archetecture.

14) File Formats?
=> It is how data is store in a file.
2 types file formats
--------------------
=> Row Based
=> Column Based

15) Delta Format
=>